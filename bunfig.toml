[test]

# Keep generated JavaScript in build/ out of test discovery.
root = "./tests"

# load test environment variables before any test runs
preload = ["./tests/setup.ts"]

# always enable coverage
coverage = true
coverageReporter  = ["text"]
